{% extends 'base.html.twig' %}

{% block title %}Subjects{% endblock %}

{% block body %}

<div class="subject-main">
    <h1 class="subject-title">{{ subject.label}}</h1>

    <p>This subject in part of {{ subject.category.label }}'s category</p>

    <a href="{{ path("edit_subject", {"id" : subject.id}) }}">
        <p>Edit</p>
    </a>

    {# <a href="{{ path("delete_student", {"id" : student.id} ) }}">
        <p>Delete</p>
    </a> #}

</div>

{% endblock %}