{% extends 'base.html.twig' %}

{% block title %}Subjects{% endblock %}

{% block body %}

<div class="session-main">
    <h1 class="general-title">{{ student.firstname ~ " " ~ student.lastname}}</h1>

    {# <a class="general-edit-button" href="{{ path("edit_student", {"id" : student.id}) }}">
        <img class="general-edit-button-icon" src="{{ asset("img/icon_edit.png") }}" alt="Edit icon">
        <p>Edit</p>
    </a>

    <a class="general-delete-button" href="{{ path("delete_student", {"id" : student.id} ) }}">
        <img class="general-delete-button-icon" src="{{ asset("img/icon_trash.png") }}" alt="Edit icon">
        <p>Delete</p>
    </a> #}

</div>

{% endblock %}