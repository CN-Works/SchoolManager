{% extends 'base.html.twig' %}

{% block title %}Students{% endblock %}

{% block body %}

<div class="student-main">
    <h1 class="generalt-title">All students</h1>

    <a class="general-new-button" href="{{ path("new_student") }}">
        <p>Create a new student</p>
    </a>

    <div class="general-cards">
        {% for student in allstudents %}
            <div class="general-card">
                <figure class="general-card-banner unselectable">
                    <img class="general-card-banner-image" src="{{ asset("img/default_image.jpg") }}" alt="{{ student.firstname ~ " " ~ student.lastname }}'s banner image">
                </figure>

                <h2 class="general-card-label">{{ student.firstname ~ " " ~ student.lastname }}</h2>
                <p class="general-card-description">{{  student.firstname ~ " from "~ student.city }}</p>
                <a class="general-card-infobutton" href="{{ path("show_student", {"id" : student.id}) }}">
                    <p>See more</p>
                </a>
            </div>
        {% endfor %}
    </div>
</div>

{% endblock %}
